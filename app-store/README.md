# App Store release metadata

The localized App Store listing lives in `metadata/`:

- `app-info/` contains app-level fields such as the name, subtitle, and privacy URL.
- `version/` contains release fields such as the description, keywords, and support URL.

The release binary is built from the public `v1.0.0` tag and exposes that exact source URL in Settings → Licence.
